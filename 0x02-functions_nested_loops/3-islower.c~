#include "main.h"
/**
*_islower: Returns 1 if c is lowercase and 0 if otherwise
*int c: Returns 1 if c is lowercase
*Return: always 0
*/
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
